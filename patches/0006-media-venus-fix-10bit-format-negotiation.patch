diff --git a/drivers/media/platform/qcom/venus/helpers.c b/drivers/media/platform/qcom/venus/helpers.c
index 5425d1840ad4..4c235dd6739c 100644
--- a/drivers/media/platform/qcom/venus/helpers.c
+++ b/drivers/media/platform/qcom/venus/helpers.c
@@ -39,47 +39,45 @@ struct intbuf {
 	u32 dpb_out_tag;
 };
 
-bool venus_helper_check_codec(struct venus_inst *inst, u32 v4l2_pixfmt)
+u32 venus_helper_get_codec(u32 v4l2_pixfmt)
 {
-	struct venus_core *core = inst->core;
-	u32 session_type = inst->session_type;
-	u32 codec;
-
 	switch (v4l2_pixfmt) {
 	case V4L2_PIX_FMT_H264:
-		codec = HFI_VIDEO_CODEC_H264;
-		break;
+	case V4L2_PIX_FMT_H264_NO_SC:
+		return HFI_VIDEO_CODEC_H264;
 	case V4L2_PIX_FMT_H263:
-		codec = HFI_VIDEO_CODEC_H263;
-		break;
+		return HFI_VIDEO_CODEC_H263;
 	case V4L2_PIX_FMT_MPEG1:
-		codec = HFI_VIDEO_CODEC_MPEG1;
-		break;
+		return HFI_VIDEO_CODEC_MPEG1;
 	case V4L2_PIX_FMT_MPEG2:
-		codec = HFI_VIDEO_CODEC_MPEG2;
-		break;
+		return HFI_VIDEO_CODEC_MPEG2;
 	case V4L2_PIX_FMT_MPEG4:
-		codec = HFI_VIDEO_CODEC_MPEG4;
-		break;
+		return HFI_VIDEO_CODEC_MPEG4;
 	case V4L2_PIX_FMT_VC1_ANNEX_G:
 	case V4L2_PIX_FMT_VC1_ANNEX_L:
-		codec = HFI_VIDEO_CODEC_VC1;
-		break;
+		return HFI_VIDEO_CODEC_VC1;
 	case V4L2_PIX_FMT_VP8:
-		codec = HFI_VIDEO_CODEC_VP8;
-		break;
+		return HFI_VIDEO_CODEC_VP8;
 	case V4L2_PIX_FMT_VP9:
-		codec = HFI_VIDEO_CODEC_VP9;
-		break;
+		return HFI_VIDEO_CODEC_VP9;
 	case V4L2_PIX_FMT_XVID:
-		codec = HFI_VIDEO_CODEC_DIVX;
-		break;
+		return HFI_VIDEO_CODEC_DIVX;
 	case V4L2_PIX_FMT_HEVC:
-		codec = HFI_VIDEO_CODEC_HEVC;
-		break;
+		return HFI_VIDEO_CODEC_HEVC;
 	default:
-		return false;
+		return 0;
 	}
+}
+EXPORT_SYMBOL_GPL(venus_helper_get_codec);
+
+bool venus_helper_check_codec(struct venus_inst *inst, u32 v4l2_pixfmt)
+{
+	struct venus_core *core = inst->core;
+	u32 session_type = inst->session_type;
+	u32 codec = venus_helper_get_codec(v4l2_pixfmt);
+
+	if (!codec)
+		return false;
 
 	if (session_type == VIDC_SESSION_TYPE_ENC && core->enc_codecs & codec)
 		return true;
diff --git a/drivers/media/platform/qcom/venus/helpers.h b/drivers/media/platform/qcom/venus/helpers.h
index 7df71ce00e8e..21436a6b7e19 100644
--- a/drivers/media/platform/qcom/venus/helpers.h
+++ b/drivers/media/platform/qcom/venus/helpers.h
@@ -11,6 +11,7 @@
 struct venus_inst;
 struct venus_core;
 
+u32 venus_helper_get_codec(u32 v4l2_pixfmt);
 bool venus_helper_check_codec(struct venus_inst *inst, u32 v4l2_pixfmt);
 struct vb2_v4l2_buffer *venus_helper_find_buf(struct venus_inst *inst,
 					      unsigned int type, u32 idx);
diff --git a/drivers/media/platform/qcom/venus/hfi.c b/drivers/media/platform/qcom/venus/hfi.c
index f6adc2bbf68d..6e8bd99450ff 100644
--- a/drivers/media/platform/qcom/venus/hfi.c
+++ b/drivers/media/platform/qcom/venus/hfi.c
@@ -14,39 +14,10 @@
 #include "hfi.h"
 #include "hfi_cmds.h"
 #include "hfi_venus.h"
+#include "helpers.h"
 
 #define TIMEOUT		msecs_to_jiffies(1000)
 
-static u32 to_codec_type(u32 pixfmt)
-{
-	switch (pixfmt) {
-	case V4L2_PIX_FMT_H264:
-	case V4L2_PIX_FMT_H264_NO_SC:
-		return HFI_VIDEO_CODEC_H264;
-	case V4L2_PIX_FMT_H263:
-		return HFI_VIDEO_CODEC_H263;
-	case V4L2_PIX_FMT_MPEG1:
-		return HFI_VIDEO_CODEC_MPEG1;
-	case V4L2_PIX_FMT_MPEG2:
-		return HFI_VIDEO_CODEC_MPEG2;
-	case V4L2_PIX_FMT_MPEG4:
-		return HFI_VIDEO_CODEC_MPEG4;
-	case V4L2_PIX_FMT_VC1_ANNEX_G:
-	case V4L2_PIX_FMT_VC1_ANNEX_L:
-		return HFI_VIDEO_CODEC_VC1;
-	case V4L2_PIX_FMT_VP8:
-		return HFI_VIDEO_CODEC_VP8;
-	case V4L2_PIX_FMT_VP9:
-		return HFI_VIDEO_CODEC_VP9;
-	case V4L2_PIX_FMT_XVID:
-		return HFI_VIDEO_CODEC_DIVX;
-	case V4L2_PIX_FMT_HEVC:
-		return HFI_VIDEO_CODEC_HEVC;
-	default:
-		return 0;
-	}
-}
-
 int hfi_core_init(struct venus_core *core)
 {
 	int ret = 0;
@@ -211,7 +182,7 @@ int hfi_session_init(struct venus_inst *inst, u32 pixfmt)
 	if (inst->state != INST_UNINIT)
 		return -EALREADY;
 
-	inst->hfi_codec = to_codec_type(pixfmt);
+	inst->hfi_codec = venus_helper_get_codec(pixfmt);
 	reinit_completion(&inst->done);
 
 	ret = ops->session_init(inst, inst->session_type, inst->hfi_codec);
diff --git a/drivers/media/platform/qcom/venus/vdec.c b/drivers/media/platform/qcom/venus/vdec.c
index ceffba2b5c84..f2354951a0e3 100644
--- a/drivers/media/platform/qcom/venus/vdec.c
+++ b/drivers/media/platform/qcom/venus/vdec.c
@@ -378,9 +378,15 @@ static int vdec_s_fmt(struct file *file, void *fh, struct v4l2_format *f)
 	inst->crop.width = inst->width;
 	inst->crop.height = inst->height;
 
-	if (f->type == V4L2_BUF_TYPE_VIDEO_OUTPUT_MPLANE)
+	if (f->type == V4L2_BUF_TYPE_VIDEO_OUTPUT_MPLANE) {
 		inst->fmt_out = fmt;
-	else if (f->type == V4L2_BUF_TYPE_VIDEO_CAPTURE_MPLANE) {
+		/*
+		 * Capture format enumeration is filtered through hfi_codec.  Keep it
+		 * in sync as soon as userspace selects the compressed format instead
+		 * of waiting until the HFI session is created.
+		 */
+		inst->hfi_codec = venus_helper_get_codec(fmt->pixfmt);
+	} else if (f->type == V4L2_BUF_TYPE_VIDEO_CAPTURE_MPLANE) {
 		inst->fmt_cap = fmt;
 		inst->output2_buf_size =
 			venus_helper_get_framesz(pixfmt_cap, orig_pixmp.width, orig_pixmp.height);
@@ -1451,6 +1457,12 @@ static void vdec_buf_done(struct venus_inst *inst, unsigned int buf_type,
 	vb = &vbuf->vb2_buf;
 
 	if (type == V4L2_BUF_TYPE_VIDEO_CAPTURE_MPLANE) {
+		if (IS_IRIS1(inst->core) && !inst->sequence_cap)
+			dev_info(inst->core->dev_dec,
+				 "venus-test8: first capture buffer type=%#x tag=%u bytes=%u offset=%u fmt=%#x hfi_flags=%#x\n",
+				 buf_type, tag, bytesused, data_offset,
+				 inst->fmt_cap->pixfmt, hfi_flags);
+
 		vb2_set_plane_payload(vb, 0, bytesused);
 		vb->planes[0].data_offset = data_offset;
 		vb->timestamp = timestamp_us * NSEC_PER_USEC;
@@ -1501,6 +1513,14 @@ static void vdec_event_change(struct venus_inst *inst,
 
 	mutex_lock(&inst->lock);
 
+	if (inst->bit_depth != ev_data->bit_depth) {
+		inst->bit_depth = ev_data->bit_depth;
+		if (inst->bit_depth == VIDC_BITDEPTH_10)
+			inst->fmt_cap = &vdec_formats[VENUS_FMT_P010];
+		else
+			inst->fmt_cap = &vdec_formats[VENUS_FMT_NV12];
+	}
+
 	format.type = V4L2_BUF_TYPE_VIDEO_CAPTURE_MPLANE;
 	format.fmt.pix_mp.pixelformat = inst->fmt_cap->pixfmt;
 	format.fmt.pix_mp.width = ev_data->width;
@@ -1534,17 +1554,17 @@ static void vdec_event_change(struct venus_inst *inst,
 	inst->out_width = ev_data->width;
 	inst->out_height = ev_data->height;
 
-	if (inst->bit_depth != ev_data->bit_depth) {
-		inst->bit_depth = ev_data->bit_depth;
-		if (inst->bit_depth == VIDC_BITDEPTH_10)
-			inst->fmt_cap = &vdec_formats[VENUS_FMT_P010];
-		else
-			inst->fmt_cap = &vdec_formats[VENUS_FMT_NV12];
-	}
-
 	if (inst->pic_struct != ev_data->pic_struct)
 		inst->pic_struct = ev_data->pic_struct;
 
+	if (IS_IRIS1(inst->core))
+		dev_info(dev,
+			 "venus-test8: source change %ux%u bit_depth=%#x profile=%#x level=%#x capture_fmt=%#x size=%u stride=%u\n",
+			 ev_data->width, ev_data->height, ev_data->bit_depth,
+			 ev_data->profile, ev_data->level, inst->fmt_cap->pixfmt,
+			 format.fmt.pix_mp.plane_fmt[0].sizeimage,
+			 format.fmt.pix_mp.plane_fmt[0].bytesperline);
+
 	dev_dbg(dev, VDBGM "event %s sufficient resources (%ux%u)\n",
 		sufficient ? "" : "not", ev_data->width, ev_data->height);
 
