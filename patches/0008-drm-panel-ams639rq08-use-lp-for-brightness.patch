diff --git a/drivers/gpu/drm/panel/panel-samsung-ams639rq08.c b/drivers/gpu/drm/panel/panel-samsung-ams639rq08.c
index f8ebbd4a5..da6312877 100644
--- a/drivers/gpu/drm/panel/panel-samsung-ams639rq08.c
+++ b/drivers/gpu/drm/panel/panel-samsung-ams639rq08.c
@@ -204,14 +204,15 @@ static int ams639rq08_bl_update_status(struct backlight_device *bl)
 	u16 brightness = backlight_get_brightness(bl);
 	int ret;
 
-	dsi->mode_flags &= ~MIPI_DSI_MODE_LPM;
-
+	/*
+	 * Keep brightness updates in low-power mode. Forcing the 0x51 DCS
+	 * write into HS mode can race a TE-triggered command-mode pixel burst
+	 * and starve all four DSI HS lane FIFOs on Raphael.
+	 */
 	ret = mipi_dsi_dcs_set_display_brightness_large(dsi, brightness);
 	if (ret < 0)
 		return ret;
 
-	dsi->mode_flags |= MIPI_DSI_MODE_LPM;
-
 	return 0;
 }
 
